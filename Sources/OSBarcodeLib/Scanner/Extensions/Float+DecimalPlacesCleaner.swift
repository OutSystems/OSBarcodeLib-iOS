extension Float {
    /// Presents a clean text value, when the decimal places are ".0"
    var clean: String {
       return self.truncatingRemainder(dividingBy: 1) == 0 ? String(format: "%.0f", self) : String(self)
    }
}
